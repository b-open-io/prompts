{
  "output": "## Full Stack Recommendation for 500k IoT Data Points\n\n### 1. Chart Types\n\n| Panel | Chart | Why |\n|-------|-------|-----|\n| Temperature / Humidity / Pressure over time | **Multi-line + area** (one panel per metric) | Time series >20 points → line, not column |\n| Per-device comparison | **Heatmap** (device × time bin) | 50 categories — bar fails, heatmap shines |\n| Distribution snapshots | **Box plot** per device or per hour | Catch anomalies that line charts hide |\n\nDon't try to show all 50 device lines simultaneously — that's 150 lines of noise. Use a device filter + aggregate view by default, drill to individual devices.\n\n---\n\n### 2. Library: Apache ECharts (not Recharts, not Chart.js)\n\n500k points puts you squarely in the **10k–500k interactive tier**:\n\n```\nScale        Library            Why\n─────────────────────────────────────────────────────\n<10k         Recharts/Chart.js  SVG fine, simple API\n10k–500k     Apache ECharts ✓   Canvas, LTTB built-in, 60fps zoom/pan\n500k–10M+    LightningChart     GPU/WebGL required — overkill here\n```\n\nECharts gives you:\n- `sampling: 'lttb'` — Largest Triangle Three Buckets downsampling (visually lossless)\n- Built-in `dataZoom` component (slider + scroll wheel + drag to pan)\n- Canvas renderer (not SVG) — handles 500k points without frame drops\n- `large: true` mode on line series for bulk rendering\n\n---\n\n### 3. Data Pipeline\n\n**Problem:** Sending 500k raw rows to the browser kills load time and memory.\n\n**Solution: Two-tier loading**\n\n```sql\n-- Tier 1: Overview (always loaded, ~2k rows)\nSELECT\n  device_id,\n  metric,\n  date_trunc('hour', ts) AS hour,\n  avg(value)             AS avg_val,\n  min(value)             AS min_val,\n  max(value)             AS max_val\nFROM sensor_readings\nWHERE ts >= now() - interval '6 months'\nGROUP BY 1, 2, 3\nORDER BY 3;\n\n-- Tier 2: Drill-down (loaded on zoom, scoped to window)\nSELECT device_id, metric, ts, value\nFROM sensor_readings\nWHERE ts BETWEEN :start AND :end\n  AND device_id = ANY(:device_ids)\nORDER BY ts;\n```\n\n**Materialization:** Create the Tier 1 hourly aggregate as a materialized view, refresh every 15 min. Tier 2 queries hit the raw table with a `(device_id, ts)` composite index — fast for narrow time windows.\n\nIf you're on DuckDB or ClickHouse, Tier 1 runs in <100ms even without a materialized view.\n\n---\n\n### 4. ECharts Implementation\n\n```tsx\nimport ReactECharts from 'echarts-for-react';\n\nconst option = {\n  renderer: 'canvas',          // MUST be canvas at this scale\n  dataset: { source: rows },\n  dataZoom: [\n    { type: 'inside', xAxisIndex: [0, 1, 2] },   // scroll to zoom\n    { type: 'slider',  xAxisIndex: [0, 1, 2] },  // range slider\n  ],\n  series: [{\n    type: 'line',\n    large: true,               // bulk rendering mode\n    sampling: 'lttb',         // visually lossless downsampling\n    symbol: 'none',           // no dots at 500k points\n    encode: { x: 'ts', y: 'avg_val' },\n  }],\n  // Link all three metric panels to the same x-axis zoom:\n  axisPointer: { link: [{ xAxisIndex: 'all' }] },\n};\n```\n\n**Linked zoom across panels** is the key UX win — zoom the temperature chart and humidity/pressure snap to the same window automatically.\n\n---\n\n### 5. On-Zoom Data Swap\n\nWhen the user zooms into a <24h window, swap from hourly aggregates to raw data:\n\n```ts\nchart.on('datazoom', ({ start, end }) => {\n  const windowMs = (end - start) / 100 * totalRangeMs;\n  if (windowMs < 24 * 60 * 60 * 1000) {\n    fetchRawData(startTs, endTs, selectedDevices).then(raw => {\n      chart.setOption({ dataset: { source: raw } });\n    });\n  }\n});\n```\n\nThis pattern keeps initial load fast (<50ms render of 2k aggregated rows) while providing full fidelity on zoom.\n\n---\n\n### 6. Dashboard Layout\n\n```\n┌─────────────────────────────────────────────┐\n│  Device filter: [All] [Dev-01] [Dev-02] ...  │\n│  Time range: [6mo] [1mo] [1w] [custom]       │\n├─────────────────────────────────────────────┤\n│  Temperature (°C)      ────────── [zoom bar] │\n│  Humidity (%)          ────────── [linked]   │\n│  Pressure (hPa)        ────────── [linked]   │\n├─────────────────────────────────────────────┤\n│  Heatmap: device × day (anomaly detection)   │\n└─────────────────────────────────────────────┘\n```\n\n---\n\n### Summary Decision\n\n| Concern | Answer |\n|---------|--------|\n| Library | **Apache ECharts** with Canvas renderer |\n| Downsampling | **LTTB** (`sampling: 'lttb'`) |\n| Data strategy | Hourly aggregates for overview, raw on drill-down |\n| Zoom/pan | ECharts `dataZoom` — linked across all panels |\n| Backend | Materialized hourly view + raw table with composite index |\n| Avoid | Recharts, Chart.js, any SVG-based renderer |\n\nThe single biggest win is the materialized hourly aggregate — it reduces your payload from 500k to ~4,320 rows (50 devices × 3 metrics × ~2,160 hours per 90-day window) with zero visible quality loss at the overview zoom level.",
  "tokens": 2260,
  "duration_ms": 59706,
  "assertions": [
    {
      "id": "ch-2-canvas-not-svg",
      "text": "The response explicitly recommends Canvas or WebGL rendering instead of SVG for this data volume, or explains why SVG would fail at 500k points.",
      "passed": true,
      "reasoning": "The response explicitly recommends Canvas renderer over SVG, states 'renderer: canvas // MUST be canvas at this scale', and notes ECharts uses 'Canvas renderer (not SVG) — handles 500k points without frame drops'."
    },
    {
      "id": "ch-2-downsampling",
      "text": "The response mentions downsampling, data aggregation, or level-of-detail strategy — showing an overview with reduced points and full detail on zoom/drill-down.",
      "passed": true,
      "reasoning": "The response recommends LTTB downsampling via ECharts' built-in 'sampling: lttb', a two-tier data strategy with hourly aggregates for overview (~2k rows) and raw data on zoom/drill-down, and even provides the on-zoom data swap implementation."
    },
    {
      "id": "ch-2-appropriate-library",
      "text": "The response recommends a library capable of handling 500k+ points with interactive pan/zoom — such as ECharts, LightningChart, Plotly with WebGL, or similar high-performance option. Does not recommend Chart.js, Recharts, or basic D3 as the primary solution.",
      "passed": true,
      "reasoning": "The response recommends Apache ECharts as the primary solution for the 500k-point scale, explicitly dismisses Recharts and Chart.js as insufficient, and mentions LightningChart for even larger scales — all appropriate guidance."
    }
  ],
  "pass_rate": 1
}