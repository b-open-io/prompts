{
  "name": "Lint on Session Start",
  "description": "Runs bun lint on session start to populate initial lint status for statusline display",
  "version": "1.0.0",
  "note": "Only activates if project has package.json with lint or lint:fix script. Requires jq and bun. Runs silently - output not shown to Claude. Works with statusline.sh to display lint counts.",
  "customizable": true,
  "requires": ["jq", "bun"],
  "hook": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "~/.claude/hooks/lint-on-start.sh"
          }
        ]
      }
    ]
  }
}
