{
  "name": "Lint on Save",
  "description": "Runs bun lint:fix after file edits and stores error/warning counts for statusline display",
  "version": "1.0.0",
  "note": "Only activates if project has package.json with lint:fix script. Requires jq and bun. Runs silently - output not shown to Claude. Works with statusline.sh to display lint counts.",
  "customizable": true,
  "requires": ["jq", "bun"],
  "hook": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "~/.claude/hooks/lint-on-save.sh"
          }
        ]
      }
    ]
  }
}
