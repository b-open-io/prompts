# OrdFS-directory collections — implementation plans

Generated by the `improve` skill, 2026-07-15. **Plans only — every plan remains TODO; do not implement any step until that plan is reviewed and approved.** Each plan targets a different repo and is self-contained (an executor with zero prior context can run it from the plan file + the repo). After approval, execute through the agent framework with the model selected by the operator.

## The one principle every plan encodes

Ord-fs directories / references are a **content-delivery layer at the item level**, orthogonal to collection semantics. The collection stays an `image/*` MAP `subType:collection` ordinal (AIP-signed); items stay MAP `subType:collectionItem` with every existing field (`collectionId`, `mintNumber`, `rank`, `rarityLabel`, `traits`, `attachments`; AIP-signed). **Only the item's inscription content changes** — from embedded image bytes to a reference resolving to a shared **or** per-item image. Result: dedup for large mints **and** granular per-output images, with **zero change** to the data shape indexers/DBs/UIs consume.

## Encoding decision (settled 2026-07-15)

The three content-reference encodings are not rival alternatives — they are different layers. **Produce two, accept three:**

- **`ref=ordfs` — the pointer primitive** (produce). The item declares the source's real MIME (e.g. `image/png`) and carries a one-hop pointer as a media-type parameter. Default for a single shared or unique image. A first-class OrdFS capability, spec'd in `content-ref.md`.
- **`ord-fs/json` — the container** (produce). Only when an item bundles ≥2 named leaves (image + hi-res + metadata + attachments); its leaves may themselves be refs. A single-entry directory is redundant with `ref=ordfs`.
- **`text/uri-list` — read-only interop** (accept only). Redundant with `ref=ordfs` for production (same job, loses the MIME type); the resolver accepts it for interop but the SDK/app never emit it.

**Membership never reads the leaf.** A referenced item is byte-identical at the MAP layer, so membership keys on `collectionId` + `subType:collectionItem` + AIP — encoding is fully orthogonal. There is no collection indexer yet, so plan 003's listing DEFINES this rule.

## Confirmed backend facts (cited across plans)

1sat-stack `pkg/ordfs` supports: `ord-fs/json` traversal, outpoint + relative-vout (`_N`) pointers, 0-sat bitcom-`B` leaf content by outpoint, and `{outpoint}:-1` latest for the 1-sat root (spend-chain crawl); it loads txs via `beef.Storage` and parses on the fly (does not gate on the output index). Caveats: `beef`/`spends` chains must include a remote (JungleBus) fallback; leaves must be bitcom-`B` (not raw OP_RETURN); `:-1` is 1-sat-only (B leaves and directory-traversed leaves resolve pinned).

## Execution order & status

| Plan | Title | Repo | Priority | Effort | Depends on | Status |
|------|-------|------|----------|--------|------------|--------|
| [001](./001-1sat-sdk-ordfs-dir-and-collectionitem-ref.md) | OrdFS dir entry API + `mintCollectionItem({ ref })` | 1sat-sdk (PR #13 branch `feat/ordfs-directory-writing`) | P1 | M | — | TODO |
| [002](./002-1sat-ordinals-docs-reference-shared-content.md) | Docs: reference-shared-content pattern | 1sat-ordinals | P2 | S | — | TODO |
| [003](./003-1sat-stack-verify-and-collection-listing.md) | Verify beef/spends fallback + B-leaf serving; decide member-listing API | 1sat-stack | P2 | S/M | — | TODO |
| [004](./004-mintflow-reference-items-and-mint-on-buy.md) | Reference item minting (drop `x-ordfs=alias`) + Mint-on-Buy | mintflow | P2/P3 | M/L | 001 | TODO |
| [005](./005-update-ordfs-skills.md) | Update OrdFS + mintflow skills (final) | bsv-skills, 1sat-sdk | P3 | S | 001–004 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (rationale).

## Dependency notes

- **001 is the foundation** — it publishes the SDK `ref` primitive that 004 (mintflow) consumes. 004 Part A STOP-conditions on 001 being published.
- **002 and 003 can run in parallel with 001** — docs and backend verification don't depend on the SDK code. But 003's live B-leaf finding gates 004 (if the resolver doesn't serve references in prod, 004 Part A is blocked).
- **005 is last** — skills must document what shipped, not the plan.

## How these become PRs / PR-comments

- **001** → the finalizing commits on 1sat-sdk PR #13 (`feat/ordfs-directory-writing`). The PR-comment draft that carries the research + design decision to the maintainers is [`_pr13-comment-draft.md`](./_pr13-comment-draft.md) (post it once the docs PR from 002 is open, filling in `<DOCS_PR_URL>`).
- **002** → a docs PR to `BitcoinSchema/1sat-ordinals` (use the repo's own `docs:` commit convention, not internal OPL). The section text is [`_reference-shared-content-section.md`](./_reference-shared-content-section.md).
- **003** → a verification finding (ops action item) + a conditional listing-API PR to 1sat-stack.
- **004** → a mintflow PR (branch TBD with operator).
- **005** → skill updates + plugin version bumps in bsv-skills / 1sat-sdk.

## Open design questions to resolve with the maintainers (before/while executing 001)

1. **Does the indexer admit a reference-content item as a collection member? — SETTLED.** There is no collection indexer yet, so plan 003's listing DEFINES the rule: membership keys on MAP `collectionId` + `subType:collectionItem` + AIP, never the inscription body. A `ref=ordfs`, `ord-fs/json`, `text/uri-list`, or `base64Content` item is byte-identical at the MAP layer, so all are admitted identically with rarity/traits intact. Purely additive. (Cite in the PR #13 comment.)
2. **NFT collection vs fungible BSV-21 pass (upstream of these plans).** These plans assume the item is a distinct NFT `collectionItem`. If the units are interchangeable — identical hold-to-access passes, e.g. the bopen.ai Licenses — a BSV-21 token is a simpler, already-specified model: deploy carries the image once, every minted unit is the token, resale is native transfer, and no collection spec or membership indexer is needed. The only material on-chain difference is wallet display (a fungible token balance vs an NFT tile in collection items), which turns on whether units are distinct or interchangeable. Decide per SKU; the two models can coexist in one product line. These plans cover only the NFT-collection path.
3. **Root: classic vs dual-role.** These plans keep the collection root a classic `image/*` MAP collection (max compat) and apply references only at the item level. If the maintainers want the root to *be* an ord-fs directory (single tradeable tree), that's a follow-up plan — it needs the root to still surface a referenced image preview + carry `subType:collection` MAP.
4. **Signing:** items stay AIP (collection authorship, matching key) even with reference content; the SIGMA/AIP-by-output-type rule is the ordfs content-signing convention and is orthogonal to collection membership. Confirm.

## Reference material (in this folder)

- [`_reference-shared-content-section.md`](./_reference-shared-content-section.md) — the exact docs text for plan 002.
- [`_pr13-comment-draft.md`](./_pr13-comment-draft.md) — the PR #13 comment (fill in `<DOCS_PR_URL>` before posting).

## Execution findings

No execution findings exist yet because every plan is **TODO**. Executors must
record plan 003's deployment checks here before changing its status, including
the tested host, fallback configuration, B-leaf result, and collection-listing
decision. Never turn an unverified assumption into a confirmed finding.

## Findings considered and rejected

- *Change the collection/collectionItem MAP spec to natively express references* — REJECTED. Many on-chain collections already use the current shape; indexers/DBs/UIs key off it. References are a content-layer concern, not a metadata-shape change.
- *Rely on MintFlow's `x-ordfs=alias` trick* — REJECTED as-is, but adopted in refined form. `x-ordfs=alias` is superseded by `ref=ordfs` — the same "outpoint is the content" idea, now a spec'd OrdFS capability (`content-ref.md`) with absolute cross-tx pointers and proper headers. Plan 004 replaces the alias trick with `ref=ordfs`.
- *Pick one encoding as the single winner* — REJECTED. The 2–1 bench split resolved into a layered role split, not a winner: `ref=ordfs` (produce, pointer primitive), `ord-fs/json` (produce, multi-leaf container), `text/uri-list` (accept-only interop). Produce two, accept three.
